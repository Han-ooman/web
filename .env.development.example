# Development - SSR memakai URL staging absolut (default env.ts),
# browser memakai /api/v1 via proxy vite.config.ts (same-origin, tanpa CORS).
# VITE_API_BASE_URL sengaja tidak diset agar default per-runtime aktif.
VITE_APP_URL=http://localhost:5173
VITE_APP_NAME=Kamus Sambas (Dev)
